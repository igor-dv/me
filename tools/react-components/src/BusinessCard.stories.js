import React from 'react';
import { storiesOf } from '@storybook/react';
import BusinessCard from './BusinessCard';

storiesOf('components/BusinessCard', module).add('simple', () => <BusinessCard />);
